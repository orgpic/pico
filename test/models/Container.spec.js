import { expect } from 'chai';
import Sequelize from 'sequelize';
import Container from '../../models/Container.js';

describe('Container table', function() {

  before(function(done) {
    done();
  });
  
  it('should have a new container when container is added', function(done) {
    done();
  });

  it('should update a container', function(done) {
    done();
  });

  it('should not contain a container after container is removed', function(done) {
    done();
  });
});